## 2026-06-23 TestRun 功能完善

- [前端] `TestRun` 導覽入口調整為 pill/tab 樣式，TestRun 列表頁改為以主要按鈕開啟新增流程。
- [前端/後端] `TestRun` 列表新增進度百分比、Passed/Total、Failed、In Progress、Pending、Created 與 Actions 欄位。
- [前端/後端] 新增 TestRun 改為彈窗式流程，可依 `Product/Version > Module > TestCase` 階層篩選並使用 checkbox 多選。
- [前端/後端] 新增 TestRun 彈窗中的 TestCase 清單顯示 Priority，並可在同一彈窗預覽 Preconditions、Step、Expected Result 與 Remark。
- [前端/後端] `TestRun` 明細頁新增狀態統計卡、進度條、Priority、Updated 與 TestCase 預覽操作。
- [前端/後端] 新增 `/testruns/<id>/report` HTML report 頁面，包含 TestRun 摘要、進度、狀態統計與完整 TestCase 結果，支援瀏覽器列印或另存 PDF。
- [後端] `GET /api/testcases/hierarchy` 新增回傳階層式 TestCase 資料，供 TestRun 建立流程使用。
- [後端] `TestRun` API 回傳新增 `total_count`、`passed_count`、`failed_count`、`in_progress_count`、`pending_count` 與 `progress_percent` 統計欄位。
- [前端] Header 導覽調整為 `TestCase` 與 `TestRun` 雙 pill，`TestCase` 點擊後進入 Product/Version 列表。
- [前端] 新增 TestRun 彈窗預覽改為右側滑入側邊面板，描述欄位改為置於 TestRun 名稱下方並保留三行高度，底部操作固定顯示 `取消 / 保存`。
- [前端] TestRun 主頁 Actions 的明細按鈕文字改為 `Detail`，Created 欄位名稱改為 `Created_Time`。
- [前端] TestRun 主頁列表移除 Description 欄位，Passed/Total、Failed、In Progress、Pending 欄位改為置中顯示。
- [前端/後端] TestRun 主頁依 `created_at` 以 `yyyy-mm` 月份分段顯示，無建立時間的資料歸入 `未分類`。
- [前端] TestRun Report 頁移除 `TestRun Report` 標題下方的 TestRun 名稱與描述副標題。

## 2026-06-19 UI 規格更新

- `Module/TestCase` 頁面中，Module 卡片標題與 TestCase 欄位列之間不再顯示分隔線。
- `Module/TestCase` 頁面中，側邊面板修改模式移除欄位卡片樣式；側邊面板預覽模式仍保留卡片樣式。
- `Module/TestCase` 頁面中，側邊面板修改模式的必填檢核改為只保留 `Case` 欄位，其他欄位不再顯示必填標記或進行必填檢核。
- `Module/TestCase` 頁面中，每個 Module 卡片右上角新增刪除 Module 圖示按鈕，與新增 TestCase 的 `+` 圖示並列顯示。
- `Module/TestCase` 頁面刪除 Module 前會顯示確認提示；若該 Module 底下已有 TestCase，提示需明確告知 TestCase 會一併刪除。
- `Module/TestCase` 頁面刪除 Module 成功後，會即時移除該 Module 卡片、更新 Product/Version 標題旁的 TestCase 總數，並在沒有 Module 時顯示空狀態且停用新增 TestCase 按鈕。
- `Product/Version` 與 `Module/TestCase` 頁面共用的修改圖示改為以 CSS 水平鏡像顯示，使圖示方向左右相反。
- `Module/TestCase` 頁面無 Module 時，`新增 TestCase` 按鈕顯示灰色 disabled 狀態且滑鼠游標為禁止符號；有 Module 時維持可點擊主色按鈕。
- `Module/TestCase` 頁面的 `新增 Module` 按鈕改為與 `新增 TestCase` 相同的主色按鈕樣式。
- Header 查詢按鈕調整為與搜尋輸入框一致的膠囊形狀，並使用與 `新增 TestCase` 相同的主色按鈕顏色。
- `Module/TestCase` 頁面的新增 Module 彈窗移除 `Module *` 欄位文字，只保留 Module 名稱輸入框。
- `Module/TestCase` 頁面的左箭頭返回按鈕移除外框、背景與陰影，改為無外框深色 chevron 符號，返回 Product/Version 頁面的功能維持不變。
- `Module/TestCase` 頁面的左箭頭返回 chevron 符號縮小視覺尺寸，點擊範圍維持不變。

## 2026-06-19 API 規格更新
- `DELETE /api/modules/{id}` 刪除 Module 成功時回傳 JSON 成功訊息，刪除不存在的 Module 時回傳 `404 Module not found`。

## 2026-06-18 UI 規格更新

- `Module/TestCase` 頁面中，每個 Module 的 TestCase 清單新增 `Case`、`Priority`、`Update`、`Action` 欄位，`Update` 以 `yyyy-mm-dd` 格式顯示。
- `Module/TestCase` 頁面中，TestCase 清單的 `Case` 內容改為一般字重，不再顯示粗體。
- `Module/TestCase` 頁面的 Product/Version 標題移除 Module 數量，只在標題同列顯示總 TestCase 數，並以不同 CSS 區分標題與統計文字。
- `Module/TestCase` 頁面的返回 Product/Version 操作改為簡約左箭頭，放在 Product/Version 標題左側並與標題同列顯示。
- `Module/TestCase` 頁面中，每個 Module 卡片的 TestCase 數量改為放在 Module 標題右側，並以不同 CSS 區分標題與統計文字。
- `Module/TestCase` 頁面的返回箭頭調整為符合頁面風格的圖示按鈕樣式，包含邊框、背景、陰影與 hover 狀態。
- `Module/TestCase` 頁面新增 TestCase 彈窗的必填欄位改為只保留 `Case`，其他欄位不再進行必填檢核。
- `Module/TestCase` 頁面修正新增 Module 與新增 TestCase 彈窗右上角 `X` 無法關閉的問題。
- `Module/TestCase` 頁面中，每個 Module 卡片右上角的 `+` 新增 TestCase 改為在該 Module 的 TestCase 清單內插入草稿列，可保存或取消，不再開啟新增 TestCase 彈窗。
- `Module/TestCase` 頁面中，新增 Module 與新增 TestCase 彈窗底部的新增、取消按鈕改為靠右排列。
- `Product/Version` 頁面中，修改圖示移除按鈕外框，只保留修改圖示本身。
- `Module/TestCase` 頁面中，每個 Module 區塊的新增 TestCase 按鈕改為沿用 `Product/Version` 頁面的簡約 `+` 圖示樣式。
- `Module/TestCase` 頁面中，Module 區塊內的 TestCase 列表改為沿用 `Product/Version` 頁面的分隔線清單樣式。
- `Module/TestCase` 頁面中，Module 卡片的 Case 標題右側新增修改圖示，並沿用 `Product/Version` 頁面的修改圖示樣式；點擊後可在同列直接修改 Case 名稱、保存或取消。
- `Module/TestCase` 頁面的 Module 卡片 Action 按鈕與 `Case` 草稿輸入框縮小，`Product/Version` 頁面的項目名稱草稿輸入框也同步縮小，以強化清單內容的可視性。
- `Product/Version` 頁面的項目列表與 `Module/TestCase` 頁面的 Module 卡片內 TestCase 清單調整為更接近管理清單樣式，統一列距、淡化分隔線與 hover 底色，並降低 Module 卡片的視覺厚重感。

## 2026-06-16 UI 規格更新

- `Product/Version` 頁面的 `新增` 按鈕改為簡約 `+` 圖示，並維持與取消按鈕一致的簡潔按鈕風格。
- `Product/Version` 點擊 `+` 後，會在項目列表最上方插入一筆新項目的編輯列，沿用既有編輯模式；垃圾桶按鈕會在完成新增後才顯示。
- `Product/Version` 頁面右側的 `x 個 Product/Version` 數量文字已移除。
- `Product/Version` 項目列表中的刪除按鈕改為更簡約的垃圾桶符號，樣式大小與修改按鈕一致，底色與外框顏色也與修改圖示一致。
- `Product/Version` 進入編輯模式時，刪除按鈕同樣固定顯示於每列最右側。
- `Product/Version` 項目列表中的刪除按鈕垃圾桶符號改為粗體顯示。
- `Module/TestCase` 頁面中的新增 Module、 新增 TestCase 與 Module 卡片 Action 按鈕，以及 header 的查詢按鈕，統一調整為方形原角按鈕風格，並使 Action 欄位對齊預覽按鈕。

## 2026-06-16 系統功能更新

- `Product/Version` 名稱現已支援重複，每次新增都會產生新的記錄而非檢查是否存在。
- 新增資料庫重置功能，用於從 GitHub 下載後重新開始使用。
  - `POST /api/admin/reset` - 清空所有資料與重置 ID 序列（不可逆）
- OpenAPI 規格文件 (openspec.yaml) 已更新，新增重置 API 端點文檔。
- API 文檔頁面 (http://127.0.0.1:5000/api) 已更新，可查看完整的重置 API 使用說明。
- 基於離線使用場景，已移除 CORS 驗證機制。系統僅支援同源請求 (localhost)。

## 2026-06-15 UI 規格更新

- `Product/Version` 頁的項目列表改為清單模式呈現，以分隔線區分各項目。
- `Product/Version` 新增彈窗右上角的 `X` 可正常關閉彈窗，且新增、取消按鈕改為靠右排列。
- `Product/Version` 修改流程取消彈窗，改為在清單項目中直接編輯現有名稱，並於名稱右側顯示 `保存` 按鈕。
- `Product/Version` 頁面列表右側顯示 `x 個 Product/Version` 數量文字。
- `Product/Version` 修改按鈕改為圖示，並移動至項目名稱右側。
- `Product/Version` 點擊修改後的名稱輸入框寬度調整為原本的一半。
- `Product/Version` 保存按鈕改為綠色勾勾圖示，並移動至輸入框右側。
- `Product/Version` 修改模式新增紅色 `X` 取消按鈕，位於輸入框與保存按鈕之間。
- `Product/Version` 修改模式點擊輸入框外區域時，會自動視為取消修改。
- 系統 header 左上角 `TestCase 管理系統` 連結改為 `Product/Version` 導覽連結，並移除重複的 `Product/Version` 導覽項目。
- `Product/Version` 每個項目標題前方新增資料夾符號。
- `Product/Version` 可點擊項目 hover 時移除底線，改以整行淺灰底作為明顯標示。
- `Product/Version` 列表項目的上下可點擊範圍略為加大，延伸到分隔線附近。
- `Product/Version` 淺灰 hover 區域本身可直接點擊進入對應頁面。

## 2026-06-15 Product/Version 與 Module/TestCase 結構調整

- `TestCase` 主頁改為 `Product/Version` 列表，維持列表瀏覽，但不再顯示原本的欄位名稱。
- `Product/Version` 頁提供新增、修改、刪除功能，點擊項目後進入對應的 `Module & TestCase` 頁面。
- `Module & TestCase` 頁沿用既有 TestCase 瀏覽樣式，改為依 `Product/Version` 分組顯示各 `Module` 與其 `TestCase`。
- `TestCase` 新增與修改改為使用 `Module` 下拉選單，來源為當前 `Product/Version` 底下的 `Module`。
- `TestCase` 新增與修改移除 `Product/Version` 文字欄位，預覽功能維持不變。
- 既有後端 CRUD 與 SQLite 資料結構沿用，僅調整前端入口與規格文件。

## 2026-06-01 TestRun 規格更新

- 系統主畫面與導覽列中的 `Project` / `Projects` 顯示文字統一改為 `TestRun`。
- 專案相關頁面標籤改為 `TestRun`，包含新增欄位名稱與頁面標題。
- Web 路由由 `/projects` 系列調整為 `/testruns` 系列，包含列表、建立、明細與狀態更新。
- REST API 路由由 `/api/projects` 系列調整為 `/api/testruns` 系列，並同步更新 `/api/index` 回傳的入口名稱。
- SQLite 資料表由 `projects`、`project_test_cases` 調整為 `testruns`、`testrun_test_cases`。

## UI 規格更新

- `TestCase 管理` 頁面移除標題下方說明文字。
- TestCase 列表改為表格呈現，欄位為 `Product`、`Module`、`Case`、`Priority`、`update`，其中 `update` 以 `yyyy-mm-dd` 格式顯示。
- 列表操作統一提供 `編輯`、`修改`、`刪除` 按鈕。
- 頁尾移除 `簡約風格 TestCase 管理系統 / SQLite + Python` 文案。
- `新增 TestCase` 表單中的 `Case 標題` 改為 `Case`。
- `新增 TestCase` 與 `編輯 TestCase` 表單新增 `Remark` 欄位。
- `Remark` 欄位排序調整為表單與預覽側邊面板的最後一列。
- `Priority` 預設值為 `Medium`。
- 新增、修改、刪除提示改為懸浮通知，顯示 3 秒後自動消失。
- TestCase 資料新增 `updated_at` 欄位，用於列表 `update` 顯示與更新時間追蹤。
- `TestCase 管理` 頁面 Header 中央新增搜尋列，可對 `Case` 進行模糊查詢。
- `TestCase 管理` 預覽側邊面板新增 `Remark` 顯示，並可點擊 `修改` 後直接在側邊面板編輯所有欄位；此時才會顯示 `保存` 按鈕，保存成功後會回到唯讀模式並隱藏 `保存` 按鈕。
- `TestCase 管理` 列表與預覽側邊面板唯讀模式中，欄位值若為空會顯示 `N/A`。
- `新增 TestCase` 與預覽側邊面板編輯模式的必填欄位為 `Product`、`Module`、`Case`、`Step`、`Expected Result`，欄位標籤會顯示紅色 `*`，且失焦時若為空會在欄位下方顯示紅字 `請輸入欄位`。
- `新增 TestCase` 頁面點擊 `保存` 時，會一次檢核所有必填欄位；若有空值，所有對應欄位都會同時顯示紅字 `請輸入欄位`，並於頁面中間上方顯示 `請先完成必填欄位` 提示。
- `TestCase 管理` 主內容區寬度已加大，並針對 `Product`、`Module`、`Case` 欄位調整為不自動換行，降低新建資料在列表中折行的情況。
- `TestCase 管理` 預覽側邊面板寬度由原本約三分之一調整為五分之二，以提供更充足的預覽與編輯空間。
