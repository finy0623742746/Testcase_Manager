# AGENTS.md

## 專案共通規範

- 以此檔作為本專案的持久性開發規則。
- README 只保留安裝、啟動、功能總覽與文件導引；維護規則不要寫回 README。
- 若規則與現況衝突，以本檔、`規格紀錄.md`、`api-spec.yaml`、`SYSTEM_MINDMAP.md` 的一致性為優先。

## 規格紀錄寫入規則

- `規格紀錄.md` 只記錄實際改動，不寫流程說明以外的雜項內容。
- 規格紀錄採三層結構：
  - 第一層：`# 規格紀錄`
  - 第二層：`## yyyy-mm-dd`
  - 第三層：`### 主題`
- 日期由新到舊排列；同日期的新主題放在較上方，同主題的新條目放在該主題最上方。
- 只記錄當天實際改動；不可回頭改寫過去日期的既有紀錄。
- 若今天的改動取代或修正舊規格，請在今天日期下新增條目說明，不要直接覆蓋歷史內容。
- 無法確認日期的既有內容統一放在 `## 未標日期`，並置於所有日期區段之後。
- 第三層主題需同時對應「功能範圍」與「改動類型」。
- 只有功能範圍與改動類型都相同時，才可放入當日既有主題；否則需新增第三層主題。
- 一次調整若跨越多個獨立頁面或系統層級，需拆成多個第三層主題。
- 每個規格項目前方需標示影響範圍：`[前端]`、`[後端]` 或 `[前端/後端]`。
- 若內容有變動或移除，已記錄內容也要相對應調整，避免規格紀錄與實作不一致。
- 純 BUG 修正若只是恢復既有規格，不新增規格紀錄；若修正同時改變既有行為或規格，才更新對應條目。

## 文件同步規則

- 新增或調整 API 時，必須同步更新 `api-spec.yaml`。
- 完成功能調整時，需同步檢查並更新 `SYSTEM_MINDMAP.md` 與 README 的功能摘要。
- 若變更會影響 API 契約、使用者操作或功能範圍，需同步確認 `api-spec.yaml`、`SYSTEM_MINDMAP.md`、`規格紀錄.md` 與 README 是否一致。

## 規格心智圖規則

- `SYSTEM_MINDMAP.md` 只描述使用者可操作的功能與結果，不記錄實作細節、檔名、資料庫結構或維護流程。
- `SYSTEM_MINDMAP.md` 的分類以使用者操作為準，例如：`Product/Version 管理`、`Module/TestCase 管理`、`TestRun 管理`、`Report 與輸出`、`系統輔助功能`。
- `SYSTEM_MINDMAP.md` 的節點內容以操作行為為主，例如瀏覽、新增、修改、刪除、搜尋、預覽、更新狀態、追加或移除關聯、下載 PDF。
- 若某內容屬於技術實作或維護規則，例如 Fetch/XHR、分頁實作、SQLite、schema、`.env`、快取或文件維護流程，應移到 `AGENTS.md`、`規格紀錄.md` 或其他合適文件，不寫入 `SYSTEM_MINDMAP.md`。
- 若功能範圍改變，需同步調整節點名稱或分類，避免心智圖與實際操作面不一致。

## 前端與資料規則

- 新功能或新頁面的後端資料必須透過 Fetch/XHR API 載入，不可直接以靜態內容取代動態資料流程。
- 純前端錯誤必須輸出到瀏覽器 Console，且保留足以定位問題的錯誤資訊。
